<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tpsoft.android.calculate"
    android:versionCode="2"
    android:versionName="Trapezoid" >
    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />
    <uses-permission android:name="android.permission.INTERNET"/>
  	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:icon="@drawable/ic_launcher"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" 
             android:screenOrientation="portrait">
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
      	
 		<activity android:name="com.google.ads.AdActivity"
              android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"/>
 		<activity android:name="CircleActivity"></activity>
 		<activity android:name="SphereActivity"></activity>
 		<activity android:name="TriangleActivity"></activity>
 		<activity android:name="SquareActivity"></activity>
 		<activity android:name="CylindricalActivity"></activity>
 		<activity android:name="ConeActivity"></activity>
 		<activity android:name="TrapezoidActivity"></activity>
    </application>

</manifest>